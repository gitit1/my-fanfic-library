import React from 'react';
import Button from '@material-ui/core/Button';

const ReadingList = (props) => {
    const {fanfic} = props;

    return(
        <div className='userData'>
            <Button  onClick={} color='primary' >Add to Reading List</Button>                                
        </div>
    )
};

export default ReadingList;